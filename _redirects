# Fantasy Run For A Million — Cloudflare Pages redirect rules

# Legacy /enter route → canonical pick-your-team
/enter          /pick-your-team     301

# Trailing slash normalisation (Cloudflare Pages handles natively)

# 404 — Cloudflare Pages serves 404.html automatically when present
# Uncomment below only if SPA-style client routing is ever needed:
# /*             /index.html         200

# /disciplines hub → riders (no standalone disciplines hub page)
/disciplines        /riders           301

# Top riders sub-pages (Cloudflare Pages serves these natively)
# No redirects needed — static files at top-riders/{disc}/index.html

# Strategy cluster (static HTML pages)
# Cloudflare Pages serves these natively from strategy/{page}/index.html
